# Global settings
user nginx;
worker_processes auto;

# HTTP context settings
http {
    # Include MIME types, this line is typically included by default
    include       mime.types;
    default_type  application/octet-stream;

    # Other HTTP-level settings
    # ...

    # Lua package path (if using Lua)
    lua_package_path '/path/to/lua/scripts/?.lua;;';

    # Server blocks
    server {
        listen 80;
        # server configuration...
    }

    # Other server blocks as needed
    # ...
}
